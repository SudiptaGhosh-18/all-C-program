#include<stdio.h>
int gcd(int m,int n)
{
	if(n==0)
	return m;
	else 
	return gcd(n,m%n);
}
int main()
{
	int m,n;
	printf("ENTER TWO DIGITS:");
	scanf("%d%d",&m,&n);
	printf("%d is GCD",gcd(m,n));
	return 0;
}
